name: Roadmap Checks
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  roadmap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: echo "Roadmap checks placeholder ✅"
